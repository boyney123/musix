coverage:
  status:
    project:
      default: off
      songs-client:
        target: 90%
        flags: songs-client
      songs-api:
        target: 90%
        flags: songs-api

flags:
  songs-api:
    paths:
      - songs-api/
  songs-client:
    paths:
      - songs-client/

comment:
  layout: diff
  require_changes: yes
